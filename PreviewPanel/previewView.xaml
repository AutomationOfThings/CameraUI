<UserControl x:Class="PreviewPanel.PreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PreviewPanel"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <UserControl.Resources>
        <!-- A customized slider -->
        <SolidColorBrush x:Key="HorizontalSliderTrackNormalBackground" Color="Gray"/>
        <LinearGradientBrush x:Key="HorizontalSliderTrackNormalBorder" EndPoint="0,1" StartPoint="0,0">
            <GradientStop Color="{Binding modeColors.ModeColor_White_Black}" Offset="0.1"/>
            <GradientStop Color="{Binding modeColors.ModeColor_White_Black}" Offset=".9"/>
        </LinearGradientBrush>
        <Style x:Key="SliderRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Rectangle Fill="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CustomThumbForSlider" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Ellipse Fill="{Binding modeColors.ModeColor_LightGray_Gray}" Stroke="{Binding modeColors.ModeColor_Black_WhiteSmoke}" Height="15" Width="15"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MyCustomStyleForSlider" TargetType="{x:Type Slider}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TickBar x:Name="TopTick" Visibility="Collapsed" Fill="{TemplateBinding Foreground}" Placement="Top" Height="4" Grid.Row="0"/>
                                <TickBar x:Name="BottomTick" Visibility="Collapsed" Fill="{TemplateBinding Foreground}" Placement="Bottom" Height="4" Grid.Row="0"/>
                                <Border x:Name="TrackBackground" 
                                        Background="{StaticResource HorizontalSliderTrackNormalBackground}"
                                        BorderBrush="{StaticResource HorizontalSliderTrackNormalBorder}"                                        
                                        BorderThickness="1" CornerRadius="1"
                                        Margin="5,0" VerticalAlignment="Center" Height="4.0" Grid.Row="1" >
                                    <Canvas Margin="-6,-1">
                                        <Rectangle Visibility="Hidden" x:Name="PART_SelectionRange" Height="4.0"
                                                   Fill="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"
                                                   Stroke="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}"
                                                   StrokeThickness="1.0"/>
                                    </Canvas>
                                </Border>
                                <Track x:Name="PART_Track" Grid.Row="1">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton  Style="{StaticResource  SliderRepeatButtonStyle}" Command="{x:Static Slider.DecreaseLarge}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource SliderRepeatButtonStyle}" Command="{x:Static Slider.IncreaseLarge}"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb x:Name="Thumb" Style="{StaticResource CustomThumbForSlider}" Background="Black"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid Width="500" Height="500">
        <Label x:Name="Notice" Visibility="{Binding Idle}" Content="Select Camera to Preview" HorizontalAlignment="Center" VerticalAlignment="Center" Width="359" FontFamily="Segoe UI Semibold" FontWeight="Bold" FontSize="30" Foreground="#FFCFCCCC" Background="{x:Null}" />
        <Grid Background='Black' Opacity="1" Visibility="{Binding Active}" Width="400" Height="300" Margin="30,0,0,50" HorizontalAlignment="Center" VerticalAlignment="Center"></Grid>
        <Image Source="{Binding CurrentCamera.VideoSource}" Margin="65,74,35,126" />
        <Button x:Name="zoomOut" Command="{Binding ZoomCommand}" CommandParameter="{Binding Decrease}" Visibility="{Binding Active}" HorizontalAlignment="Left" Opacity="0.7" Margin="425,337,0,0" VerticalAlignment="Top" Width="26" Height="24" Background="White">
            <Image Source="Resources/minus.png" />
        </Button>

        <Button x:Name="zoomIn" Command="{Binding ZoomCommand}" CommandParameter="{Binding Increase}" Visibility="{Binding Active}" HorizontalAlignment="Left" Opacity="0.7" Margin="425,313,0,0" VerticalAlignment="Top" Width="26" Height="24" Background="White">
            <Image Source="Resources/plus.png" />
        </Button>
        <!--
        <Button x:Name="Down" Command="{Binding TiltCommand}" CommandParameter="{Binding Decrease}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="394,130,0,0" VerticalAlignment="Top" Height="26" RenderTransformOrigin="0.5,0.5" Width="26" Background="{Binding modeColors.ModeColor_WhiteSmoke_Gray}">
            <Image Source="Resources/arrow-down.png"/>
        </Button>
        <Button x:Name="Left" Command="{Binding PanCommand}" CommandParameter="{Binding Increase}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="175,274,0,0" VerticalAlignment="Top" Height="26" Width="26" Background="{Binding modeColors.ModeColor_WhiteSmoke_Gray}">
            <Image Source="Resources/arrow-left.png"/>
        </Button>
        <Button x:Name="Right" Command="{Binding PanCommand}" CommandParameter="{Binding Decrease}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="200,274,0,0" VerticalAlignment="Top" Height="26" Width="26" Background="{Binding modeColors.ModeColor_WhiteSmoke_Gray}">
            <Image Source="Resources/arrow-right.png" />
        </Button>
        <Button x:Name="Up" Command="{Binding TiltCommand}" CommandParameter="{Binding Increase}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="394,105,0,0" VerticalAlignment="Top" Height="26" Width="26" Background="{Binding modeColors.ModeColor_WhiteSmoke_Gray}">
            <Image Source="Resources/arrow-up.png"/>
        </Button>
        -->
        <Label Content="{Binding CurrentCamera.Zoom}" Visibility="{Binding Active}" Width="20" Height="30" VerticalAlignment="Center" Background="Transparent" Opacity="0.6" Foreground="WhiteSmoke" HorizontalAlignment="Center" Margin="400,287,80,113"></Label>
        <Label x:Name="pan_min" Content="-180" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="47,372,0,106" VerticalAlignment="Center" FontSize="10" Height="22"/>
        <Label x:Name="pan_max" Content="180" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="452,372,0,106" VerticalAlignment="Center" FontSize="10" Height="22"/>
        <Label x:Name="tilt_max" Content="185" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="37,62,0,416" VerticalAlignment="Center" FontSize="10" Height="22"/>
        <Label x:Name="tilt_min" Content="-5" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="40,362,0,0" VerticalAlignment="Top" FontSize="10" Height="22"/>
        <Label x:Name="current_pan" Content="0" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="257,372,0,0" VerticalAlignment="Top" FontSize="10" Height="22"/>
        <Label x:Name="current_tilt" Content="90" Foreground="{Binding modeColors.ModeColor_Black_LightGray}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="41,212,0,0" VerticalAlignment="Top" FontSize="10" Height="22"/>
        <Image Source="Resources/scale.png" Visibility="{Binding Active}" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="57,332,29,83" Height="15" Width="413" />
        <Image Source="Resources/scale.png" Visibility="{Binding Active}" Stretch="Fill" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="-90,216,282,266" Height="18" Width="308" RenderTransformOrigin="0.5,0.5" >
            <Image.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Image.RenderTransform>
        </Image>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="62,0,0,66" Height="12" Width="407">
            <local:ScaleSlider Position="{Binding CurrentCamera.Pan}" Max="360" Min="0" Visibility="{Binding Active}" HorizontalAlignment="Stretch" Margin="0,-33,0,33"/>
        </Grid>
        <Grid HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-115,0,0,270" Height="12" Width="308" RenderTransformOrigin="0.5,0.5">
            <Grid.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Grid.RenderTransform>
            <local:ScaleSlider Position="{Binding CurrentCamera.Tilt, Mode=OneWay}" Max="185" Min="-5" Visibility="{Binding Active}" HorizontalAlignment="Right" RenderTransformOrigin="0.5,0.5" Width="308">
                <local:ScaleSlider.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform ScaleY="-1"/>
                        <SkewTransform/>
                        <RotateTransform/>
                        <TranslateTransform/>
                    </TransformGroup>
                </local:ScaleSlider.RenderTransform>
            </local:ScaleSlider>
            <!--<Image Source="Resources/indicator.png" Height="10" Margin="{Binding TiltPosition}" Visibility="{Binding Active}" Stretch="Uniform" HorizontalAlignment="Left" VerticalAlignment="Bottom" /> -->
        </Grid>
        <Slider Name="PanSlider" Value="{Binding SliderPan, UpdateSourceTrigger=Default}" Style="{StaticResource MyCustomStyleForSlider}" Visibility="{Binding Active}" Height="18" VerticalAlignment="Top" Margin="58,409,35,0" LargeChange="30" IsSnapToTickEnabled="True" Minimum="0" Maximum="355" />
        <Slider Name="TiltSlider" Value="{Binding SliderTilt, UpdateSourceTrigger=LostFocus}" Style="{StaticResource MyCustomStyleForSlider}" Visibility="{Binding Active}" Height="18" VerticalAlignment="Top" Margin="-143,215,329,0" LargeChange="30" IsSnapToTickEnabled="True" Minimum="-5" Maximum="185" RenderTransformOrigin="0.5,0.5" >
            <Slider.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-90"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Slider.RenderTransform>
        </Slider>
        <Border Width="430" Height="44" Visibility="{Binding Active}" VerticalAlignment="Center" HorizontalAlignment="Center" BorderThickness="0,1,0,0" BorderBrush="LightGray" Margin="39,456,31,0" />
        <!-- <Button Content="Save as New" Command="{Binding SaveSettingCommand}" CommandParameter="{Binding CurrentCamera}" Visibility="{Binding Active}" HorizontalAlignment="Left" Margin="340,315,0,0" VerticalAlignment="Top" Width="80" Background="WhiteSmoke" Height="26"/> -->
        <Button Content="Save" Command="{Binding SaveSettingCommand}" CommandParameter="{Binding CurrentCamera}" Background="{Binding modeColors.ModeColor_WhiteSmoke_Gray}" Foreground="{Binding modeColors.ModeColor_Black_WhiteSmoke}" Visibility="{Binding Active}" HorizontalAlignment="Center" Margin="385,470,35,4" VerticalAlignment="Center" Width="80" Height="26"/>








    </Grid>
</UserControl>
